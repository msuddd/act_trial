name: hello-world
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: my-step
        run: echo "Hello World!"
      - name: goaway
        run: bash hello.sh 
